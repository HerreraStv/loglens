[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "loglens"
version = "0.1.0"
description = "A small command-line tool for analyzing application log files."
requires-python = ">=3.11"

[project.scripts]
loglens = "loglens.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]